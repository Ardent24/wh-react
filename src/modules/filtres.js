export const filteredByValue = (array, value) =>
  array.filter((i) => i.firstName.toLowerCase().includes(value));

export const filteredBySelect = (array, value) => {
  const result = [];


  return result;
}