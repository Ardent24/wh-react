import React from "react";
import { Modal } from "react-bootstrap";

const CreateRowHeader = () => {
  return (
    <Modal.Header closeButton>
      <Modal.Title>Add Hours</Modal.Title>
    </Modal.Header>
  );
};

export default CreateRowHeader;
